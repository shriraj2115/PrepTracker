; Some ISPs (notably in India) block raw.githubusercontent.com, which the Electron
; binary installer normally uses. This mirror avoids that host entirely so
; `npm install` works out of the box regardless of network.
electron_mirror=https://npmmirror.com/mirrors/electron/
